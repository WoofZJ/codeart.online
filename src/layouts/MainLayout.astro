---
import "../styles/global.css";
import TitleBar from "../components/TitleBar.astro";
import IconBar from "../components/IconBar.astro";
import StatusBar from "../components/StatusBar.astro";

interface Props {
    title?: string;
    route?: string;
    header?: string;
}

const { title = "首页", route = "/", header = "首页" } = Astro.props as Props;
---

<html lang="zh" class="quiet-light">
    <head>
        <meta charset="utf-8" />
        <link rel="icon" type="image/png" href="/favicon.png" />
        <meta name="viewport" content="width=device-width" />
        <meta name="generator" content={Astro.generator} />
        <title>{title} | CodeArt.Online</title>
    </head>
    <body class="h-dvh lg:py-4 lg:px-8 flex flex-col">
        <div
            class="w-full rounded-xl mx-auto h-full shadow-lg flex flex-col overflow-clip bg-editor-bg max-w-[96rem]"
        >
            <TitleBar title={title} />
            <div
                class="flex grow items-stretch overflow-x-hidden flex-col lg:flex-row"
            >
                <IconBar route={route} />
                <div
                    class="hidden lg:w-3xs xl:w-2xs bg-sidebar-bg lg:flex flex-col items-stretch"
                >
                    <div class="h-8 flex items-center px-2 flex-shrink-0">
                        <span class="font-semibold">{header}</span>
                    </div>
                    <div class="relative grow overflow-y-auto">
                        <slot name="sidebar" />
                    </div>
                </div>
                <div class="flex flex-col grow">
                    <div
                        id="tab-title-bar"
                        class="bg-tab-title-bg h-8 flex items-stretch text-sm shrink-0 gap-1 overflow-x-auto"
                    >
                    </div>
                    <div class="grow overflow-auto">
                        <slot name="tabs" />
                    </div>
                </div>
            </div>
            <StatusBar />
        </div>
    </body>
</html>
